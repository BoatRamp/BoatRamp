apiVersion: v2
name: boatramp-operator
description: |
  The in-binary boatramp Kubernetes operator — reconciles BoatRampCluster / Site /
  Function custom resources into their workloads and drives quorum-safe Raft
  membership. Same binary as the server; no separate controller image.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# The boatramp version this chart tracks (the operator image tag default).
appVersion: "0.1.0"
keywords:
  - boatramp
  - operator
  - raft
  - edge
home: https://boatramp.dev
sources:
  - https://github.com/BoatRamp/BoatRamp
maintainers:
  - name: boatramp
